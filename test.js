function test( argument1, argument2, {
} = {
  bar: null
} ) {
  let variable = 5; const test = 5;

  console.log();

  const tab = [
    1,
    2,
    {
    }
  ];

  function tests( a, w, s, q ) {

  }

  function fh(
    a,
    b,
    n,
    l,
  ) {

  }
  function f( a, b, c ) { }

  let testd = 3 + 3; // test

  testd++;

  const abbi = "roman";
  const funct = ( e, k ) => null;

  var object2 = {
    foo: "bar",
    [ abbi ]: 42,
    "qux-lorem": true
  };

  // This is a comment with no whitespace at the beginning

  /** This comment has a newline
     */
  return `hello, ${ people.name }!`;
}

switch ( a ) {
  case 0:
    break;
  default:
    foo();
}